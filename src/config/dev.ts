/**
 * Временные инструменты разработки.
 * `DEV_GRID` — показывать сетку (поля контента и линию шапки) поверх сайта.
 * Команды пользователя «Включи сетку» / «Выключи сетку» меняют этот флаг.
 */
export const DEV_GRID = true;
