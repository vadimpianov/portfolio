# Portfolio — контекст проекта

Личный сайт-портфолио Вадима Пьянова. Только фронтенд, без бэкенда.

- Репозиторий: `vadimpianov/portfolio` (личный аккаунт GitHub, приватный)
- Старт проекта: 2026-09-25

## Глобальное правило №1 — Portfolio ≠ Arno

**Portfolio и Arno — два разных, никак не связанных проекта.**

- Не используем из Arno ничего: код, пакеты, конфиги, дизайн-токены, инфраструктуру, CI, хуки, агентов, `/build`-воркфлоу, `CLAUDE.md`, `Rules.md`.
- Не читаем и не пишем в репозиторий `arnomake/arno` в рамках этого проекта.
- Правило из Arno можно перенести, только если отдельно спросить пользователя про **конкретное** правило и получить явное «да». Молча ничего не переносим.

## Общение и язык

- Общение с пользователем — **на русском**.
- Сайт двуязычный: RU + EN. Работаем с текстами на русском, английскую версию Claude делает **сразу и параллельно** (без отдельной просьбы).
- Работаем **пошагово**: маленький шаг → показать → согласовать → следующий.

## Ключевые решения

| Тема | Решение |
|------|---------|
| Фреймворк | **Astro** + TypeScript (статическая генерация, минимум JS) |
| Контент | **MDX-файлы в репозитории** (`src/content/ru/…`, `src/content/en/…`) |
| Анимации | Их будет **много**. GSAP (+ ScrollTrigger), Lenis для плавного скролла, View Transitions для переходов между страницами. React-острова — только если конкретному компоненту это действительно нужно |
| Хостинг | **Cloudflare Pages**, бесплатный план. Всё должно укладываться в бесплатные лимиты |
| Бэкенд | Нет. Единственное исключение — крошечная edge-функция Cloudflare Pages для выбора языка (см. ниже) |
| Дизайн | Макета нет. Дизайн-библиотеку и сайт создаём **с нуля вместе, прямо в коде**: токены → компоненты → страницы. **Figma не используем вообще.** |
| Домен | Пока нет, решим позже |
| Структура сайта | Пока не определена, решаем по ходу |

## Выбор языка (i18n)

- Маршруты: `/ru/…` и `/en/…`.
- При заходе на `/` edge-функция (Cloudflare Pages Functions / middleware) смотрит страну посетителя (`request.cf.country`):
  - `RU` → редирект на `/ru/`
  - любая другая страна → редирект на `/en/`
- Есть переключатель языка. Ручной выбор запоминается (cookie) и **имеет приоритет над геолокацией**.
- Каждая страница и каждый MDX-кейс существует в обеих локалях.

## Открытые вопросы

- Структура разделов (главная, кейсы, обо мне, контакты — не утверждено)
- Домен
- Визуальный стиль, типографика, палитра — определяем вместе на этапе дизайн-библиотеки

## Следующий шаг

Скаффолд проекта (Astro + TS + MDX + i18n-роутинг + базовая настройка GSAP/Lenis) — **только после согласования с пользователем**. Перед началом коротко изложить план и дождаться «ок».
